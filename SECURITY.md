# Security

报告安全问题时不要公开账号凭据、浏览器扩展连接密钥、完整用户数据目录或私有会话。

仓库启用 GitHub Private Vulnerability Reporting 后，请使用 Security 页面的私密报告入口；未启用时，可先提交不含复现细节和敏感数据的 Issue，请维护者提供私密联系渠道。

GrokDesk 通过受限 IPC 访问本机项目。Grok Build 执行任务时具有当前用户权限；应只运行自己理解并信任的任务和技能。浏览器连接仅监听本机回环地址，并验证扩展来源与连接密钥。

正式发布前维护者应配置平台签名，核验第三方许可，并运行三系统构建。测试和安装包校验不能替代代码审查。
